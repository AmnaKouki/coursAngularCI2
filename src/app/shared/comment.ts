export class Comment{
    id : number;
    date: Date;
    message: string;

    //When the strinct mode is enabled, the constructor must be added
    //strict mode is in tsconfig.json
   /* constructor(id: number, date: Date, message: string){
        this.id = id;
        this.date = date;
        this.message = message;
    }
    */
}